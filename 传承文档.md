# 苍侠战队 RM2026 雷达机器人传承文档

## 一、项目概述

本项目为 RoboMaster 2026 赛季雷达机器人软件系统，基于 **ROS2 Humble** 作为通讯中间件，使用 **C++17** 开发。系统核心功能包括：基于 YOLO 的双层神经网络目标识别与追踪、单目相机 PnP 位置解算、地图可视化、串口与裁判系统通讯。

**硬件平台：**
- 运算端：NVIDIA Jetson（TRT 推理加速）
- 相机：海康机器人工业相机（MVS SDK V4.7.0）
- 串口：USB-TTL 与裁判系统互联

---

## 二、系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                    海康相机 (MVS SDK 直采)                          │
└─────────────────────────┬────────────────────────────────────────┘
                          │ image (cv::Mat)
                          ▼
┌──────────────────────────────────────────────────────────────────┐
│                 YOLO 识别节点 (yolov8decbytetrack)                 │
│   ┌─────────────┐    ┌──────────────┐    ┌──────────────────┐    │
│   │ 第一层网络    │───▶│ BYTETracker  │───▶│ 第二层网络        │    │
│   │ (车辆检测)    │    │ (目标追踪)    │    │ (装甲板分类)      │    │
│   └─────────────┘    └──────────────┘    └──────────────────┘    │
│                                                                   │
│   推理引擎: TensorRT (特化) / DNN (通用, 仅 YOLOv5/v8)             │
│   配置方式: YAML 文件                                              │
└─────────────────────────┬────────────────────────────────────────┘
                          │ Detection (custom msg)
                          ▼
┌──────────────────────────────────────────────────────────────────┐
│              位置解算节点 (location_node)                           │
│   ┌──────────────┐    ┌──────────────────┐    ┌──────────────┐   │
│   │ 掩码图地形    │───▶│ PnP + 高度补偿    │───▶│ 坐标系转换    │   │
│   │ 高度分区      │    │ (单目深度估算)     │    │ (右→左手系)   │   │
│   └──────────────┘    └──────────────────┘    └──────────────┘   │
│                                                                   │
│   地形分区: 地面 / 中央高地 / T形高地                                │
│   坐标系: OpenCV右手系 → DJI左手系 (红/蓝双方视角自适应)             │
└──────────┬──────────────────────────────┬────────────────────────┘
           │ parsed_topic                 │ parsed_topic
           ▼                              ▼
┌─────────────────────┐    ┌──────────────────────────────────────┐
│  位置查看 (ui_design) │    │       串口节点 (serial_node)           │
│                     │    │                                      │
│  · 小地图绘制        │    │  · 接收位置 → 整合消息包 → 发送裁判系统  │
│  · 轨迹历史显示      │    │  · 接收裁判系统 → 控制消息发送开关       │
│  · 红/蓝双方区分     │    │  · CRC8/CRC16 校验                    │
│                     │    │  · 无线电密钥解析接口(预留)             │
│                     │    │  · 库: wjwwood/serial                 │
└─────────────────────┘    └──────────────────────────────────────┘
```

**数据流总览：**

| 节点 | 订阅 Topic | 发布 Topic |
|------|-----------|-----------|
| yolov8decbytetrack | 相机图像 | `detection_topic` |
| location_node | `detection_topic` | `parsed_topic` |
| ui_design | `parsed_topic` | —（本地窗口渲染） |
| serial_node | `parsed_topic` | 裁判系统串口, `wave_level_topic` |

---

## 三、功能模块详解

### 3.1 YOLO 识别 (yolov8decbytetrack)

**代码路径：** `yolov8decbytetrack/`

**核心文件：**
- `src/inference_node.cpp` — ROS2 节点主入口，推理流水线
- `src/inference.cpp` — TensorRT / DNN 推理引擎封装
- `src/BYTETracker.cpp` — BYTETrack 多目标追踪器
- `src/STrack.cpp` — 单目标追踪状态
- `src/KalmanFilter.cpp` — 卡尔曼滤波器
- `src/HikCamera.cpp` — 海康相机采集封装

**功能说明：**

1. **双层神经网络识别** — 第一层网络检测车辆（car），第二层网络对检测到的目标进行分类（区分敌方/己方装甲板类型 B1~B7 / R1~R7）。第二层网络是识别准确率的关键。
2. **双推理引擎**：
   - **TensorRT（推荐）** — 针对特定输入输出格式的特化推理，需将模型转为 `.engine` 格式，前向为车辆后向为装甲板。仅支持经 TRT 编译的特定模型。
   - **DNN（通用）** — 基于 OpenCV DNN 模块，仅支持 YOLOv5 与 YOLOv8 的 `.onnx` 模型推理。
3. **BYTETracker 追踪器** — 用于多目标跟踪，关联帧间检测结果。当前追踪器未充分发挥作用，因分类重点在第二层网络，追踪仅用于维持 ID 一致性。
4. **模式切换** — 支持 test 模式与相机模式在运行中自由切换。
5. **YAML 配置** — 模型路径、类别名称、推理参数等通过 YAML 文件配置，无需重新编译。

**已知问题：**
- 代码结构偏"屎山"，推理引擎抽象不够泛化，TRT 和 DNN 两套推理路径耦合度高。
- BYTETracker 未发挥实质作用，后续考虑替换为 DeepSORT。
- YOLOv11 模型虽然更先进，但在当前数据集量下稳定性并未超过 YOLOv5，**不应随意更换模型**。

**依赖：** Ultralytics, TensorRT, OpenCV, CUDA, Eigen3, yaml-cpp, nlohmann-json

---

### 3.2 位置解算 (location_node)

**代码路径：** `location_node/`

**核心文件：**
- `src/location_main.cpp` — ROS2 节点，订阅检测结果并发布解算坐标
- `src/location.cpp` — `Location` 类，核心定位算法实现
- `src/location_double.cpp` — 双倍/备用定位逻辑
- `src/test.cpp` — 测试节点

**定位原理：**

1. **掩码图地形分区** — 手动绘制场地掩码图，将 28m×15m 的场地划分为三种高度层级：
   - **地面（Ground）** — Z = 0
   - **中央高地（Central Highland）** — Z = 中央高台高度
   - **T形高地（T-shaped Highland）** — Z = T形台高度
2. **深度估算** — 单目相机缺少真实深度信息。通过 OpenCV 位置关系计算函数判断接收到的像素点位于哪个地形区域，据此补齐 Z 值，再通过 PnP 逆投影计算 X、Y 世界坐标。
3. **坐标系转换** — 将 OpenCV 右手系转换为 DJI 左手系，并根据己方颜色（红/蓝）自适应转换：
   - **蓝方视角：** `X' = 2800 - X, Y' = 1500 - Y`
   - **红方视角：** `Y' = -Y`
4. **地图参考系：** 红方补给区为原点，+X 向蓝方，+Y 向红方停机坪。

**已知问题：**
- 定位算法基于厦门理工开源方案改造，本质仍是单目估算，依赖"猜测"来弥补缺失深度值。
- 后续希望引入**激光雷达**补全深度信息，或使用**双目相机**改善深度估计。

**依赖：** OpenCV, Eigen3, yaml-cpp, geometry_msgs, tutorial_interfaces

---

### 3.3 位置查看 (ui_design)

**代码路径：** `ui_design/`

**功能包名：** `ui_design_pkg`

**核心文件：**
- `src/MapVisualizer.cpp` — 地图可视化主类
- `src/MapVisualizer_test.cpp` — 测试代码

**功能说明：**

- 订阅位置解算节点的输出，在**内置小地图**（2800cm × 1500cm，按比例缩放）上绘制机器人位置。
- 支持**轨迹历史**显示（可配置历史长度）。
- 通过**人为对比比赛视频**来手动验证定位是否正确。
- 红/蓝双方颜色区分显示。

**评价：** 该部分较为完善，后续仅需根据每年规则更换地图图片和单位，无需大改。

**依赖：** OpenCV, geometry_msgs, tutorial_interfaces

---

### 3.4 串口通讯 (serial_node)

**代码路径：** `serial/`

**功能包名：** `serial_node`

**核心文件：**
- `src/serial_node.cpp` — ROS2 节点，串口收发主逻辑
- `include/serial.hpp` — 串口管理类、协议帧构建、CRC 校验
- `src/serial.cpp` / `src/serial2.cpp` / `src/serial3.cpp` — 历史迭代版本

**功能说明：**

1. **消息发送** — 接收位置解算节点发布的 `parsed_topic`，整合为完整消息包（包含所有目标位置），按裁判系统通讯协议格式通过串口发送。
2. **消息接收与控制** — 接收裁判系统下发的控制指令（如比赛阶段、密钥状态等），根据裁判系统指令控制是否发送消息。
3. **CRC 校验** — 实现 CRC8（多项式 0x2F）和 CRC16（多项式 0x1021）校验，保证数据完整性。
4. **无线电密钥解析接口（预留）** — 留有 `send_serial_key()` 接口，用于后续无线电密钥功能。
5. **调试模式** — 支持命令行手动输入调试，每 5 秒可触发手动发送。
6. **干扰波等级发布** — 以 100ms 周期发布 `wave_level_topic`。
7. **参数化配置** — 串口设备、波特率、队伍颜色、调试开关均通过 ROS2 参数声明，可经 YAML 文件覆盖。

**协议相关数据结构：**

- `RadarDecision` — 双倍易伤触发、加密等级、密钥修改标志等雷达决策信息
- `SerialPort` 基类 — CRC 计算、数据包收发、包构建
- `SerialManager` — 串口设备管理、收发线程、位置缓存

**已知问题：**
- 串口绑定尚未完成，需支持 YAML 配置文件修改。
- 稳定性有待增强。
- 后续规划将此节点升级为**传感器融合节点**，汇总所有传感器获取的完整消息，按通讯协议格式统一发送。

**依赖：** wjwwood/serial, tutorial_interfaces, geometry_msgs, std_msgs

---

### 3.5 相机标定 (calibrate) — 独立 C++ 程序

**代码路径：** `calibrate/`

**注意：** 标定程序是一个**独立的 C++ 可执行程序**，不依赖 ROS2，直接通过 CMake 构建、直接运行。它仅在使用前离线运行一次，输出外参 JSON 文件供位置解算节点读取。

**构建方式：**
```bash
cd calibrate && mkdir -p build && cd build
cmake .. && make -j$(nproc)
./calibrate
```

**核心文件：**
- `src/Calibration.cpp` — 标定程序入口（main 函数）
- `src/HikCamera.cpp` — 海康相机采集封装
- `include/Calibrate.h` — 标定类（PnP 求解、视频/图片/相机三种标定模式）
- `include/config.h` — 相机内参、畸变系数配置

**标定原理：**

1. 在场地地图上选取 **5 个已知世界坐标的参考点**：
   - 左补给区 (left_buff)
   - 右补给区 (right_buff)
   - 己方哨塔 (self_tower)
   - 敌方基地 (enemy_base)
   - 敌方哨塔 (enemy_tower)
2. 在相机画面中手动依次点击这 5 个点对应的**像素位置**。
3. 使用 OpenCV `solvePnP`（EPnP 方法）求解相机当前外参（旋转向量 rvec + 平移向量 tvec）。
4. 外参结果保存至 `json/calibrate.json`，供位置解算节点读取使用。

**三种标定模式：**
- `calibrate_with_camera()` — 实时相机画面标定
- `calibrate_with_video()` — 录制视频回放标定
- `calibrate_with_image()` — 单张图片标定

**已知问题：**
- 使用单目相机，**每次使用前必须重新标定**，流程繁琐。
- 后续希望引入激光雷达配合 ICP 算法实现**自动标定**，消除手动环节。

**依赖：** OpenCV (含 calib3d), Eigen3, 海康 MVS SDK, nlohmann-json

---

### 3.6 自定义消息接口 (tutorial_interfaces)

**代码路径：** `tutorial_interfaces/`

自定义 ROS2 消息包，定义了系统内部各节点间通讯的消息格式：

- `Detection` — 检测结果（包含目标列表、类别、像素坐标、置信度）
- `Target` — 单个目标信息（类别名、世界坐标 x/y/z）

---

## 四、构建与运行

### 4.1 环境要求

| 组件 | 版本 |
|------|------|
| OS | Ubuntu 22.04 |
| ROS2 | Humble |
| CUDA | ≥ 11.4 |
| TensorRT | ≥ 8.x |
| OpenCV | ≥ 4.x (含 contrib + calib3d) |
| Eigen3 | ≥ 3.3 |
| 海康 MVS SDK | V4.7.0 |

### 4.2 构建

```bash
cd ~/ros2_ws
colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

### 4.3 启动流程

1. **标定相机外参（每次使用前，独立程序，不依赖 ROS2）**
   ```bash
   cd calibrate/build && ./calibrate
   ```
   按提示依次点击 5 个参考点，标定结果自动保存至 `json/calibrate.json`。

2. **启动 YOLO 识别**
   ```bash
   ros2 run yolov8decbytetrack inference_node
   ```

3. **启动位置解算**
   ```bash
   ros2 run location_node location_node
   ```

4. **启动串口通讯**
   ```bash
   ros2 run serial_node serial_node
   ```

5. **（可选）启动位置查看**
   ```bash
   ros2 run ui_design_pkg MapVisualizer
   ```

---

## 五、后续改进方向

| 模块 | 优先级 | 改进内容 |
|------|--------|---------|
| **YOLO 识别** | 高 | 重构代码结构，抽象推理引擎接口，降低 TRT/DNN 耦合度 |
| **YOLO 识别** | 高 | 将 BYTETracker 替换为 DeepSORT，提升追踪质量 |
| **YOLO 识别** | 中 | 在数据集充足前，不随意更换模型（v11 稳定性不优于 v5） |
| **位置解算** | 高 | 引入激光雷达补全深度信息，或使用双目相机改善单目局限性 |
| **位置解算** | 高 | 探索更优秀的定位算法，摆脱对厦理开源方案的依赖 |
| **串口通讯** | 高 | 完成串口 YAML 配置绑定，增强运行稳定性 |
| **串口通讯** | 中 | 升级为传感器融合节点，汇总全部传感器消息后统一发送 |
| **相机标定** | 高 | 使用激光雷达 + ICP 算法实现自动标定，消除每次手动标定 |
| **位置查看** | 低 | 根据新赛季规则更换地图图片与单位即可 |
| **整体** | 中 | 完善错误处理和日志系统，提升调试效率 |

---

## 六、关键注意事项

1. **赛前检查清单（代码中注释）：** 上电插主控 → 查传感器端高度、运算端长宽高 → 走安全杂项流程 → 接官方主控连服务器 → 检录激光 → 断 gimbal 后激光关闭 → 最后重新上电插主控 → 预检录检查小电脑/主机是否为开源操作系统。

2. **坐标轴方向：** 地图坐标系以红方补给区为原点，+X 向蓝方，+Y 向红方停机坪。位置解算节点内已完成 OpenCV 右手系 → DJI 左手系的转换。

3. **单目局限性：** 整个位置解算系统建立在单目相机的 PN P估算之上，Z 值通过地形掩码图猜测补全，本质上是近似解，不是精确测量。

4. **模型不稳定性：** 在数据量不足的情况下，更先进的模型（如 YOLOv11）不一定带来更好效果。模型升级需要配套的数据集支撑。

5. **标定不可跳过：** 相机每重新架设后外参都会改变，必须先标定再使用，否则位置解算结果无效。

---

*文档基于苍侠战队 2026 赛季雷达机器人代码仓库整理，最后更新时间 2026-05-26。*
